<div class="container-layout">
    <div class="card-image-top">
        <img src="{{image_url}}" alt="">
    </div>
<div class="card-body">
    <h2>{{this.name}}</h2>
    <p>{{this.description}}</p>
    <h3>{{this.tagline}}</h3>
</div>

</div>